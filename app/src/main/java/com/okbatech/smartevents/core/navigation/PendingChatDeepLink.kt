package com.okbatech.smartevents.core.navigation

/** A chat thread to navigate straight to once resolved — see [EvenroNavHost] and MainActivity. */
data class PendingChatDeepLink(val threadId: String, val title: String)
